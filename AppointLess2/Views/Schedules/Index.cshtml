@model IEnumerable<AppointLess2.Schedule>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>


<p>
    @Html.ActionLink("Create New", "Create", "Schedules")
    @*@Html.ActionLink("Builder", "ScheduleBuilder", "Edit", null, null)*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
@*<th>
        @Html.DisplayNameFor(model => model.AspNetUser.Email)
    </th>*@
        <th></th>
    </tr>

    @foreach(var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
@*<td>
        @Html.DisplayFor(modelItem => item.AspNetUser.Email)
    </td>*@
            <td>
                @*@Html.ActionLink("Edit", "Edit", "Schedules", new { id = item.Id }, null) |
                @Html.ActionLink("Details", "Details", "Schedules", new { id = item.Id }, null) |*@
                @Html.ActionLink("Poista", "Delete", "Schedules", new { id = item.Id }, null) |
                @Html.ActionLink("Muokkaa", "Edit", "ScheduleBuilder", new { id = item.Id }, null) 
            </td>
    </tr>
    }

    </table>
